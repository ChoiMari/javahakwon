-- 한 줄 주석(comment)
/*
* 블록 주석
*/
-- SQL 키워드(명령어)는 대소문자를 구분하지 않음.
-- SQL 문장은 세미콜론으로 끝남.

-- 단축키 Ctrl+Enter;
--  1. 현재 커서가 있는 위치의 한 문장을 실행
-- ;이 없으면 문장이 끝난 것이 아니다.
-- 
SELECT sysdate FROM dual;

-- bonus 테이블의 모든 레코드를 검색:
select * from bonus;

-- dept
select * from dept;


